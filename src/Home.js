import React from 'react';

import {useHistory} from 'react-router-dom';

export const Home = () => {
    return(
        <>
            <h1>
                Home
            </h1>

            <button>
                Play
            </button>
        </>
    );
};

export default Home();